import 'package:flutter/material.dart';

class AppColors {
  // ═══════════════════════════════════════════════════════════════
  // DARK MODE - Backgrounds
  // ═══════════════════════════════════════════════════════════════
  static const darkBg = Color(0xFF141B1E);
  static const darkSurface = Color(0xFF1E2528);
  static const darkSurfaceAlt = Color(0xFF232A2D);

  // ═══════════════════════════════════════════════════════════════
  // LIGHT MODE - Backgrounds (REDESIGNED - Modern Cool Tone)
  // ═══════════════════════════════════════════════════════════════
  static const lightBg = Color(0xFFFAFBFC); // Cool white background
  static const lightSurface = Color(0xFFFFFFFF); // Pure white surfaces
  static const lightSurfaceAlt = Color(0xFFF0F4F8); // Soft blue-gray

  // ═══════════════════════════════════════════════════════════════
  // DARK MODE - Text Colors
  // ═══════════════════════════════════════════════════════════════
  static const textDark = Color(0xFFDADADA);
  static const textDarkSecondary = Color(0xFFA8A8A8);
  static const textDarkTertiary = Color(0xFF7A7A7A);

  // ═══════════════════════════════════════════════════════════════
  // LIGHT MODE - Text Colors (REDESIGNED)
  // ═══════════════════════════════════════════════════════════════
  static const textLight = Color(0xFF1A1F2E); // Dark navy text
  static const textLightSecondary = Color(0xFF5A6B7A); // Medium gray
  static const textLightTertiary = Color(0xFF8A9AAA); // Light gray

  // ═══════════════════════════════════════════════════════════════
  // ACTION COLORS - Dark Mode
  // ═══════════════════════════════════════════════════════════════
  static const green = Color(0xFF8CCF7E); // Keep - good for dark
  static const blue = Color(0xFF67B0E8); // Secondary
  static const cyan = Color(0xFF6CBFBF); // Slightly increased saturation

  // ═══════════════════════════════════════════════════════════════
  // ACTION COLORS - Light Mode (REDESIGNED - More Vibrant)
  // ═══════════════════════════════════════════════════════════════
  static const greenLight = Color(0xFF10B981); // More vibrant green
  static const blueLight = Color(0xFF0EA5E9); // Vibrant sky blue
  static const cyanLight = Color(0xFF06B6D4); // Bright cyan

  // ═══════════════════════════════════════════════════════════════
  // SEMANTIC COLORS
  // ═══════════════════════════════════════════════════════════════
  static const red = Color(0xFFEF4444); // Brighter red
  static const yellow = Color(0xFFE5C76B); // Keep (dark mode)
  static const yellowLight = Color(0xFFF59E0B); // Amber (light mode)
  static const purple = Color(0xFFC47FD5);

  // ═══════════════════════════════════════════════════════════════
  // UI Elements - Light Mode
  // ═══════════════════════════════════════════════════════════════
  static const lightDivider = Color(0xFFE5E7EB); // Light gray divider
  static const lightBorder = Color(0xFFD1D5DB); // Medium gray border
  static const lightHoverBg = Color(0xFFF3F7FC); // Hover background
  static const lightDisabled = Color(0xFFD1D5DB); // Disabled state

  // ═══════════════════════════════════════════════════════════════
  // UI Elements - Dark Mode
  // ═══════════════════════════════════════════════════════════════
  static const darkDivider = Color(0xFF2A3033); // Dark divider
  static const darkBorder = Color(0xFF323A40); // Dark border
  static const darkHoverBg = Color(0xFF272E32); // Dark hover
  static const darkDisabled = Color(0xFF4A5560); // Dark disabled
}
